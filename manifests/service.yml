apiVersion: v1
kind: Service
metadata:
    name: simaxsimplejavascriptappwithdockerfile
spec:
    type: LoadBalancer
    ports:
    - port: 8080 
    selector:
        app: simaxsimplejavascriptappwithdockerfile